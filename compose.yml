name: "zx"
services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "zigx.nuhu.dev"
    restart: unless-stopped
    networks:
      - npm

networks:
  npm:
    driver: bridge
